import styled from "@emotion/styled";

const PaddedDiv = styled('div')`
  padding: 12px 24px;
  form {
    padding-bottom: 12px;
    button {
      margin-top: 12px;
    }
  }
`;

export default PaddedDiv;
